Algoritmo "Calculador de area de um retangulo"

Var
       altura, base: real

Inicio
       escreval("calculo da area do retangulo")
        escreva("Digite a medida da base do retangulo: ")
       leia(base)
       escreva("Digite a medida da altura do retangulo: ")
       leia(altura)
       area <- base * altura
       escreva("Area do retangulo é: ", area)
Fimalgoritmo